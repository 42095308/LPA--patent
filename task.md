# 说明书公式与工程实现一致性核对代办清单

核对对象：
`C:\Users\42095\Desktop\专利模板\初稿\100002说明书3.0.docx`

核对范围：
已全文通读说明书正文、实施例、系统模块描述和权利要求，并核对其中公式（1）至公式（27）以及步骤 S1-S5 对应的当前工程实现逻辑。

说明：
说明书正文中未发现单独的程序代码块，因此本次“代码工作逻辑”核对，按全文文字叙述、步骤描述、权利要求、触发条件、结构化消息内容和 EMS 预调逻辑进行比对。以下仅列出已经确认存在偏差或需要明确补充的地方；未列出的公式和流程，与当前工程基本一致，或可视为实现细化。

## 代办清单

1. 公式（3）对下降段的处理方式与代码不一致
文档位置：段落 [47]-[48]，`Pclimb = M g vclimb`
代码位置：`energy_map.py:23-27`、`energy_map.py:185-188`
差异说明：说明书公式按字面含义允许 `vclimb < 0` 时产生负爬升功率；代码实现中 `climb_rate <= 0.0` 直接返回 `0.0`，只对正爬升段计入爬升功率。
建议动作：如果当前实现是最终口径，建议在说明书里补充“爬升功率仅对上升段计入”；如果说明书口径才是最终口径，则代码需要显式处理下降段功率模型。

2. 公式（9）的氢耗项尺度与代码实现不一致
文档位置：段落 [55]-[60]，`C(n,n') = αTflight + βEhydrogen + γΨdegradation,local·kSoH(SoH)`
代码位置：`config.py:80-83`、`energy_map.py:189-192`
差异说明：说明书写成直接使用 `βEhydrogen`；代码实际使用 `config.BETA * h2_g * config.H2_COST_SCALE`，其中 `H2_COST_SCALE = 1000.0`，说明书未说明这一额外尺度因子。
建议动作：二选一，补充说明“氢耗项在代价函数中做了额外尺度归一化/放缩”；或者将代码中的独立放缩常量并入文档中的 `β` 定义。

3. 公式（13）对应的环境事件触发逻辑与代码实现不一致
文档位置：段落 [77]-[79]，`Cnew(e) > Cold(e)·rtrigger`
代码位置：`simulation.py:475-520`、`config.py:67-71`
差异说明：说明书给出的触发条件是单边代价比例阈值；代码实际流程还额外叠加了路径邻域筛选、绝对代价变化阈值 `T2_COST_DELTA_ABS_THRESHOLD`、以及“受影响边数不少于 4000 条”这一整体触发门槛。
建议动作：说明书需要补充当前工程的多重触发门控逻辑；如果专利文本必须保持单阈值表达，则代码需要收敛到该表达。

4. 公式（14）和公式（15）中的前瞻功率跃迁判据与代码不一致
文档位置：段落 [80]-[84]，`ΔPreqT3 = max(ΔPlocal, ΔPfuture)` 且 `ΔPreqT3 > ΔPstep,max`
代码位置：`simulation.py:561-599`、`config.py:57-59`
差异说明：说明书写的是前瞻窗口内“最大”功率跃迁量；代码对 `ΔPfuture` 使用 `robust_dp_req_w(..., T3_PROFILE_DP_PERCENTILE)`，默认取 90 分位而不是严格最大值。
建议动作：如果代码口径更合理，说明书应改成“基于分位数的鲁棒跃迁判据”；如果说明书口径必须是最大值，代码应改为直接使用 `max_dp_req_w`。

5. 公式（27）的燃料电池单步应力增量与代码数学形式不完全一致
文档位置：段落 [118]-[120]
代码位置：`ems.py:22-28`
差异说明：说明书公式写成 `ΔStressFC = ΔPFC/Pstep,ref + (ΔPFC/Δt)/Pramp,ref · Δt`；代码实现为 `((ΔPFC/Pstep,ref) + ((ΔPFC/Δt)/Pramp,ref)) * dt`，等价于把整个括号整体乘了 `dt`，与文档当前写法并不完全一致。
建议动作：统一公式括号和时间积分口径。要么修正文档公式表达，要么按文档公式调整代码。

6. 公式（12）和公式（16）中的在线应力/SoH 更新口径与代码不一致
文档位置：段落 [73]-[75]、[85]-[87]
代码位置：`simulation.py:323-332`、`simulation.py:602-613`
差异说明：说明书表述为在线累计“燃料电池应力”并据此更新 `SoH`，再用 `StressFC,online` 和 `SoH` 参与健康触发；当前代码在触发阶段使用的是路径需求功率跳变量 `dp_req` 来更新 `online_fc_stress_index` 和 `estimated_soh`，并不是 EMS 中的实际燃料电池输出功率变化 `ΔPFC`。
建议动作：如果专利希望强调真实燃料电池应力，应把触发前的在线应力更新改成基于 EMS 实际 `fc_power`；如果当前代码口径不变，则说明书应明确这是“基于需求功率的代理应力”。

7. 段落 [88] 关于“复用历史 g/rhs 结果”的表述与代码不一致
文档位置：段落 [88]，触发后“利用 LPA* 增量搜索规划器复用历史 g/rhs 结果进行局部重规划”
代码位置：`simulation.py:631-655`、`simulation.py:702-706`
差异说明：说明书表述为触发后统一采用复用历史 `g/rhs` 的局部重规划；代码在 `heavy_reset` 条件下会直接重建 `LPAStar` 对象，历史 `g/rhs` 会被丢弃，不再属于纯粹的历史复用。
建议动作：说明书补充“在健康权重突变较大时允许执行规划器重置”；或者移除代码中的 `heavy_reset` 重建逻辑。

8. 公式（20）的结构化消息字段定义与代码实现不一致
文档位置：段落 [96]-[99]，`{timestamp, feature_vector, Ppredict(t), Twindow}`
代码位置：`trajectory.py:140-163`、`simulation.py:798-808`
差异说明：说明书给出的消息结构只有四项；代码实际消息中额外包含 `replanning_id`，并且曲线字段名为 `P_predict`、时间窗口字段名为 `T_window`，与文档写法不完全一致。
建议动作：如果说明书要与当前工程一一对应，建议补充 `replanning_id` 并统一字段命名；如果说明书只想保留抽象结构，则代码注释或文档导出说明里应明确这是实现层命名。

9. 公式（25）的母线电压估计模型与代码实现不一致
文档位置：段落 [111]-[114]，`Vbus,actual = Vbus - Ieq Req`
代码位置：`ems.py:55-64`、`ems.py:154-157`
差异说明：说明书使用“等效供电电流 `Ieq`”的一阶等效内阻模型；代码实际使用 `V_BUS - (i_total + i_shortfall) * R`，其中还额外引入了欠供电电流 `i_shortfall` 惩罚，而且 `i_total` 与电池补偿电流计算都基于固定名义母线电压 `config.V_BUS`，并非文档字面上的 `Vbus,actual` 反馈闭环。
建议动作：统一母线电压模型口径。要么说明书补充“欠供电惩罚项”和“名义母线电压近似”；要么把代码改写成严格对应文档公式的形式。

10. 说明书写明的“障碍物信息与空域约束建图”当前工程未实现
文档位置：段落 [16]、[26]、[43]、[125]、[137]
代码位置：`energy_map.py:208-245`、`simulation.py:1193`
差异说明：说明书多处写明三维空间图构建需要引入障碍物信息和空域约束信息；当前工程实际只加载 DEM 地形与风场，并基于走廊半径、地形净空和高度范围构图，没有看到障碍物占据模型、禁飞空域约束或相应输入数据结构。
建议动作：如果当前工程是最终口径，说明书应删去或弱化“障碍物信息/空域约束”相关表述；如果说明书口径必须保留，工程需要补充障碍物和空域约束建模。

11. 说明书中的“障碍物事件触发重规划”当前代码未实现，T1 只是占位
文档位置：段落 [20]、[28]、[144]-[148]
代码位置：`simulation.py:471-472`、`simulation.py:691-699`
差异说明：说明书与权利要求均把“障碍物信息更新导致已规划路径不可通行”列为事件触发条件之一；当前代码中的 `T1` 仅返回 `reason=\"stub\"`，不会触发任何实际重规划逻辑。
建议动作：二选一，补齐障碍触发逻辑及受阻边更新；或者在说明书中删除对应触发条件，只保留当前已实现的风场/功率/健康触发。

12. 说明书中的“实时连续监测”与当前工程的固定事件时刻仿真不一致
文档位置：段落 [20]、[28]、[69]、[127]、[139]
代码位置：`config.py:108`、`simulation.py:1119-1135`
差异说明：说明书表述为飞行过程中持续实时监测环境和健康状态；当前工程按 `EVENT_INTERVALS = [15.0, 10.0, 8.0]` 预设事件时刻，在 3 个固定时间点统一评估触发条件，更接近离散事件仿真而不是连续在线监测。
建议动作：如果当前工程只是验证型仿真，说明书应明确为“离散事件触发仿真”；如果说明书坚持连续监测口径，工程需要改为基于连续观测流的实时触发机制。

13. 权利要求中“当前物理状态下允许的最大功率爬升限值”与代码固定阈值不一致
文档位置：段落 [146]
代码位置：`config.py:39`、`simulation.py:583-598`
差异说明：权利要求将功率触发阈值描述为“燃料电池在当前物理状态下所允许的最大功率爬升限值”；当前代码使用固定常量 `FC_DP_MAX_STEP = 900.0` 作为阈值，并未根据当前燃料电池状态、SoH、温度或其他物理状态动态调整。
建议动作：如果说明书口径正确，工程需要把阈值做成状态相关量；如果工程口径正确，权利要求应改为“预设固定阈值”或“状态相关阈值的简化实现”。

14. 结构化消息中的时间戳语义与说明书“重规划完成时间戳”不一致
文档位置：段落 [22]、[96]-[99]、[150]
代码位置：`simulation.py:778-785`、`simulation.py:800-807`
差异说明：说明书写明消息可包含“局部增量重规划完成时间戳”；当前代码把消息 `timestamp` 直接设置为 `trigger_time_s + CONTROL_MESSAGE_DELAY_S`，并未把实际 `planning_latency_ms` 计入该时间戳，因此它更接近“触发/发送时刻”而不是“重规划完成时刻”。
建议动作：统一时间戳语义。要么说明书改成“消息生成/发送时刻”，要么代码把 `timestamp` 调整为包含规划完成时延的时刻。

15. 说明书中的“调整电池放电电流限值”与代码直接求解电池电流不一致
文档位置：段落 [24]、[30]、[129]、[141]、[154]
代码位置：`ems.py:154-157`、`ems.py:198-202`
差异说明：说明书反复描述的是“调整锂电池的放电电流限值上限”，属于约束调节动作；当前代码没有单独的电流限值状态量，而是直接按功率缺口计算 `bat_current = p_gap / V_BUS`，传统链还乘了一个固定放大系数。
建议动作：如果说明书要保持控制器口径，工程应显式引入电池电流上限变量及其调节逻辑；如果当前工程只是等效仿真，说明书应改成“按功率缺口等效分配电池补偿电流”。
